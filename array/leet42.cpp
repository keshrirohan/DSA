#include<iostream>
#include<vector>
#include<math>
using namespace std;



    int trap(vector<int>& height) {
        int j=1;int val,int minimum;
        for(int i=0;i<height.size();i++){
            int minimum = min(height[i],height[j]);
            val 
        }
    }


    int main(    ){
            vector<int> arr={4,2,0,3,2,5};
            trap(arr);
    }